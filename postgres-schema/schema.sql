CREATE DATABASE sparkdb;

CREATE TABLE sparktb (   id INT PRIMARY KEY NOT NULL,
                         first_name  TEXT NOT NULL,
                         last_name  TEXT NOT NULL);

INSERT INTO sparktb (id, first_name, last_name) values (1, 'Nessa', 'Kennedy');

